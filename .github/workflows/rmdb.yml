on:
  workflow_dispatch:
  schedule:
    - cron: '0 0,6,19 * * ?'
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Push new cookie
        run: |
          rm /home/runner/work/garmin-sync-coros/garmin-sync-coros/db/garmin.db
